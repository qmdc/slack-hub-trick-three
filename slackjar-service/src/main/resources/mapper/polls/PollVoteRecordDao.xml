<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.slack.slackjarservice.polls.dao.PollVoteRecordDao">

    <select id="countBySurveyAndVoter" parameterType="map" resultType="int">
        SELECT COUNT(*) FROM poll_vote_record WHERE survey_id = #{surveyId} AND voter_id = #{voterId} AND deleted = 0
    </select>

    <select id="selectBySurveyId" parameterType="long" resultType="com.slack.slackjarservice.polls.entity.PollVoteRecord">
        SELECT * FROM poll_vote_record WHERE survey_id = #{surveyId} AND deleted = 0 ORDER BY vote_time DESC
    </select>

    <select id="selectByQuestionId" parameterType="long" resultType="com.slack.slackjarservice.polls.entity.PollVoteRecord">
        SELECT * FROM poll_vote_record WHERE question_id = #{questionId} AND deleted = 0 ORDER BY vote_time DESC
    </select>

</mapper>